{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "@models.dev/web",
  "type": "module",
  "scripts": {
    "dev": "bun run build && wrangler dev",
    "build": "bun run script/build.ts",
    "preview": "bun run build && wrangler dev",
    "deploy": "bun run build && wrangler deploy",
    "cf-typegen": "wrangler types --env-interface Environment"
  },
  "dependencies": {
    "hono": "^4.8.3",
    "@hono/vite-ssg": "^0.1.0"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "@aryalabs/models": "workspace:*",
    "@cloudflare/vite-plugin": "^1.2.3",
    "vite": "^6.3.5",
    "wrangler": "^4.17.0"
  }
}
